import { NativeBaseProvider, Text, Box } from 'native-base';
import { theme } from '../../utils/theme';

function FidelityScreen() {
  return (
      <Box flex={1} >
        <Text>Compte fidélité</Text>
      </Box>
  );
}

export default FidelityScreen;